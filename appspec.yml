version: 0.0
os: linux
files:
  - source: /appspec.yml
    destination: /home/ubuntu/repo/demoRepo
hooks:
  ApplicationStart:
    - location: backup.sh
      timeout: 300
      runas: root

  ValidateService:
    - location: download.sh
      timeout: 300
      runas: root
